'use strict';

/**
 * @ngdoc function
 * @name demoApp.controller:TrainingCtrl
 * @description
 * # TrainingCtrl
 * Controller of the demoApp
 */
angular.module('demoApp')
  .controller('TrainingCtrl', function ($scope) {
    	
    	Shadowbox.init({
    	handleOversize: "drag",
    	modal: true
	});
  });
